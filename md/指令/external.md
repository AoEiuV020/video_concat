
1. 接下来要做健壮性优化，先开一个分支一点一点处理， 
1. 所有操作，包括初始化和用户交互，都得在vm层捕获异常并打印和显示，要杜绝闪退崩溃
1. 然后是针对外部工具 ffmpeg/ffprobe 找出来，在一个地方封装列举，然后保证每一个工具都要在设置页允许可配置，主页进来时就要检查是否存在，必要时直接跳到设置页，让用户去设置， 
1. 初次使用时，自动根据操作系统查找几个可能存在的路径，
1. 设置页找到程序可用的话，显示版本号

1. 我本来是说实现一点提交一点的，现在都已经实现了，那就算了，整体提交一下

1. 默认路径相关单独一个文件，
1. 启动页发现工具问题直接跳转设置页， 

1. 不是这意思， 我是说 _candidatePathsForCurrentPlatform 单独一个文件，

1. 不对不对，我想要的是自动进入设置页是跟手动点击一样的效果，也就是可以返回到主页。你现在这样子都返回不到主页了
1. 把提交撤销一下，等一下一起，等我确认完了再一起提交

1. 你说什么傻话？代码没提交啊

1. 重来，提交要加载Skill git-commit 不要排除我添加的其他文件

1. 为什么改成了Stateful?有必要吗？我以为只是加个按钮，点击调用VM的函数就可以了

1. 那原本的回车键提交为什么就没有问题？就不需要是stateful

1. 怎么感觉代码有该复用的没复用啊？就算按你说的这样，那也应该可以只获取值，传一下就可以复用原有的封装吧？总之想办法封装起来复用，

1. 大概是这样，但是还是想问一下，你这个Stateful只是为了存放Controller，并没有State。Controller没办法在Stateless使用吗

1. HookConsumerWidget更合适吗？这是否更符合当前的状态管理?

1. 那就直接提交吧

1. 增加版本号，添加更新日志

1. 你这什么记忆力啊？最新的HEAD就是你刚提交的啊，总之按SKU检查近期更新，写进更新日志里

1. 刷新或者提交那里如果是空字符串，就和第一次初始化一样，加载查找默认路径,

1. 首页现在这个提示不是很靠谱啊，
1. 一方面，同样的提示好像只会提一次。后面再犯就没反应了
1. 一方面，启动初始化时，那个报错，就是工具不存在的那个报错，它没有马上弹出来，而是等出现其他错误的时候才弹出来
1. 先考虑一下有没有什么好办法，更稳定靠谱的提示，尽量还是使用Snackbar，有没有办法解决

1. 目前初始状态的变更似乎没处理好，就是启动的时候如果工具不可用的话，哪怕到设置页给它配置好了，主页依然在报工具不可用
1. 顺便工具不可用，这里不要用或，哪个不可用就说哪个，一个一个来，一个不可用就不检查第二个

1. 我怎么感觉有时候输入框里的内容会自己变掉？你确认一下，只要有保存值，不管可用不可用，无论如何都不要触发自动修改，比如自动寻找默认值啊或者什么的。只有第一次没有保存值，或者保存了空白的情况，才能触发自动探索

1. 把前面的版本号加一的提交rebase移动到最新，然后调整更新日志，amend合并

1. 你怎么总是看到不对劲的HEAD？明明啥也没动过，就是你刚提交的HEAD啊。你是怎么看的Git

1. 你怎么理解的？我说的合并是指你补上最近的更新日志，合并到之前的更新日志里面去， 不是让你把bug修复跟日志更新合并到一起去啊？拆出来
