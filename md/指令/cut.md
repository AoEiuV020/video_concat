
1. 裁剪没有效果， 
1. 一进入页面首先就没加载出预览图，
1. 进度条拖动也会自动回到0，
1. 关键是半点日志都没有，

1. 抱歉，我刚发现项目里压根没有准备日志框架, 总之日志只能使用`logger`库，参考 lib/src/log.dart 但是要安排好tag，每个模块定义一个内部使用的logger，然后只在app模块设置 Logger静态成员也就是默认配置，
1. 如果logger不会使用，记得先调研出个参考api文档，
1. 另外重写AGENTS.md要符合通常的规范并简单提及这个logger，

1. PrefixPrinter是什么鬼，设置两种不同的printer你是几个意思？

1. 那就不要tag， 日志里直接写必要的信息，
1. 我一开始说的模块本意其实是库，一个库一个Logger，全局公开，在库内部使用的公开对象

1. “定义自己的公开 Logger” 这说法有点不清晰了。我想强调的是，每个库定义一个不导出的，只在库内部使用的logger对象

1. log补充一下，针对所有值得打日志的代码 ，尤其是VM层。按理说用户触发的每一个操作都应该有try catch log。保证不崩溃，同时保证所有异常都要打

1. AGENTS.md不对， 本项目基本上大量使用melos，尽量不直接使用flutter/dart,

1. 不用flutter pub get，要melos bs

1. 库模块也酌情加一些日志

1. 不要强调Mac OS,基本上这是个跨平台项目，只是当前环境是Mac

1. 日志先提交一个commit，功能我还没测，
1. 然后异常情况应该在界面上显示出来，考虑怎么更符合mvvm，比如在vm处理提示内容字段，在view层处理提示，

1. 明显有问题就别傻等了啊， 主要是melos test会要求输入选择执行的库，
1. 你上网查一下这里要怎么指定全库执行或者单个库单个文件单个group/test执行， 然后添加一个测试专用的skill,
1. 注意melos现在的7和6以前不一样，flutter test命令参数也得参考，
