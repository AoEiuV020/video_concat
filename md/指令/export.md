
1. 我想添加导出选项功能，效果是一个复选框点击后显示其他导出选项，包括一个记住（默认不勾选，勾选记住所有导出选择，包括导出选项开关本身），
1. 主要犹豫的是ui似乎不太放得下，你先想想应该怎么设计，出个文档，不要代码，只讲设计，

1. 有点麻烦，目前只想到设置导出结果旋转，不过我突然感觉确实输出目录文件名后缀这所有似乎都是导出选项的一部分，是否应该全部合成到二级页面？

1. 确实还是应该单个页面搞定， 处理好滚动，

1. 本项目禁止一切重新编码相关的处理，不要问这个，把这点写进AGENTS.md

1. 考虑一下还能顺便加什么, ffmpeg支持什么无损的功能？

1. 注意记住时能记的都记，取消时，不保存也不修改不删除，也就是说下次启动会使用上次保存的状态，
1. 比较特别的是`记住`这个开关本身每次都要记，
1. 就在开始合并时触发记住，

1. 开始实现， 注意代码要分数，单文件不能太大，

1. 最新设计更新到`md/规划/通用`，注意不要有过时/错误/修改，只保留最终设计，
1. 设计要完整，不要有具体实现代码，单个文档不能太大，可以多拆几个文档互相引用，
1. ffmpeg的用法得最终确认一下，用浏览器或者curl，不能只凭记忆和搜索，总结文档到`md/参考`，注意有理有据要附带信息来源，

1. 旋转无效，已知`ffmpeg -i $in -c copy -metadata:s:v:0 rotate=$rotate  "$name-$rotate$ext"`这样有效，重新调研一下， 更新文档，记得有理有据，

1. 我说了，禁止用搜索作为依据，务必使用真实浏览器或者curl这类准确内容验证过链接，重来，所有没验证的内容都视为无效，

1. 我都说了`真实浏览器`，为什么一再无视我说的浏览器？
1. 而且不止是旋转， 我是说所有没验证过的都得验证，

1. 搞错了， 重新整理一下， 我当前mac上必须`ffmpeg -display_rotation $rotate -i $in -codec copy "$name-$rotate$ext"`才有效， `metadata:s:v:0`无效， 重新调研一下看是为什么， 更新文档， 
1. 最重要的是看是否有必要判断系统区分，

1. 你把0当作无？这不合适， 我需要把其他旋转角度改成0,
1. 选项的颜色和勾有点重复了， 去掉勾吧，会导致item宽度变化，
1. 不对，勾也留着，但能不能实现勾不显示的时候保持占位，

1. 我意思是让你加一个代表不设置方向，

1. md/参考/FFmpeg/无损处理参数.md:141 我看你提到冲突， 代码里有体现吗？既然冲突了肯定要在ui上体现吧，自动取消另一个？

1. 勾还是太丑了， 删除， 直接用颜色区分，

1. 提交全部到一个commit，
