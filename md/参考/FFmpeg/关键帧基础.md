# 关键帧基础

视频流由三种帧类型组成：

| 帧类型 | 全称 | 特点 |
|--------|------|------|
| I帧（关键帧） | Intra-frame | 包含完整画面，可独立解码 |
| P帧 | Predicted frame | 前向预测，依赖前面的 I/P 帧 |
| B帧 | Bi-directional frame | 双向预测，依赖前后帧 |

两个相邻 I帧之间的帧序列称为 [GOP](https://en.wikipedia.org/wiki/Group_of_pictures)（Group of Pictures）。`-c copy` 模式下只能在 I帧位置进行无损切割。

## 关键帧对齐

由于 inter 编码的限制，直接使用非关键帧时间作为裁剪点会导致：

| 问题 | 原因 |
|------|------|
| 起始处多余帧 | `-c copy` 从前一个关键帧开始复制 |
| 结尾处多余帧 | PTS 超过 outpoint 的 B/P 帧被包含 |
| 相邻片段重叠 | inpoint 前的额外包与上一个文件的尾部重叠 |

**解决方案**：用 ffprobe 预先探测关键帧，将裁剪点对齐到关键帧时间戳。对齐后，上述问题不存在。
